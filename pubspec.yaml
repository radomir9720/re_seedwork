name: re_seedwork
description: Reusable base classes and interfaces.

version: 0.1.4

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc: ^8.1.0
  meta: ^1.7.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

 
